mixin newses(newses)
  h3.newses__title Новости коллектива:
  each news in newses
    .newses__news.news
      p.news__name= news.title
      p.news__text= news.text
      .news__date Дата публикации: #{news.date}
